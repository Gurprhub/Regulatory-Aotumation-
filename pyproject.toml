[project]
name = "regulatory-automation"
version = "0.1.0"
description = "Regulatory compliance management for agrochemical companies"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = ["error::DeprecationWarning:app.*"]
